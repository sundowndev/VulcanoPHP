{% extends 'admin/layout.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <div class="jumbotron">
                <h3 class="display-3">Bonjour, {{ session.username }}!</h3>
                <p class="lead">Bienvenue sur votre tableau de bord, c'est ici que vous allez pouvoir gérer votre site ainsi que son contenu. Cet espace d'administration, dont vous seul pouvez y accèder, vous permet de gérer vos photos et vidéos, vos catégories ainsi que la configuration générale de votre site.</p>
                <hr class="my-4">
                <p>Afin de bien débuter, voici quelques liens utiles que vous retrouverez à tout moment dans le menu latéral gauche.</p>
                <p class="lead">
                    <a href="{{ paths.admin }}/create/article" class="btn btn-app">
                        <i class="fa fa-plus"></i> Article
                    </a>

                    <a href="{{ paths.admin }}/create/category" class="btn btn-app">
                        <i class="fa fa-plus"></i> Category
                    </a>

                    <a href="{{ paths.admin }}/configuration" class="btn btn-app">
                        <i class="fa fa-cog"></i> Configuration
                    </a>

                    <a href="{{ paths.admin }}/settings" class="btn btn-app">
                        <i class="fa fa-wrench"></i> Settings
                    </a>
                </p>
            </div>

        </div>
    </div>
{% endblock %}